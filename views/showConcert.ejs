<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include ('./partials/head.ejs') %>
	</head>
	<body>
		<div class="Show">
			 <a href="/"> <button> Back to Home</button></a>
            <h2>Artist: <%= concert.name %></h2>
            <h3>Tour Name: <%= concert.tourName %></h3>
			<p>
				<% if (concert.image) { %>
				<img class="image" src= "<%= concert.image %>" width ="300" /> 
                <% } else { %>
				<img class= "image" src = "/noimage.jpeg" width="300" />
				<% } %>
			</p>
            <p>
                Genre: <%= concert.genre %>
            </p>
            <p> Concert Date: <%= concert.dateAttended %>
            </p>
            <p>Notes: <%= concert.notes %>
            </p>
            <!-- Function Buttons-->
           
            <a href ="/concert/edit/<%= concert._id %>"> 
         <button type = "submit"> Edit Concert </button>
            </a>

            <button id="deleteArtistModal" onclick="document.querySelector('.modal').style.display='inherit'"> Delete Concert</button> 

            <!-- The Modal -->
                            <div id="myModal" class="modal">
                              <!-- Modal content -->
                                <div class="modal-content">
                                <span class="close" onclick="document.querySelector('.modal').style.display=''">&times;</span>
                                <p>Are you sure you want to delete <%= concert.name %>:<%= concert.tourName %>?</p>
                                <div><form style = "display: inline;" action="/concert/<%= concert._id %>?_method=DELETE" method ="post"> <input type="hidden" name="_method" value="DELETE"> 
                                    <button type="submit">Yes</button> </form>
                        <button onclick="document.querySelector('.modal').style.display=''">No</button></div>
		</div>
	</body>
</html>
